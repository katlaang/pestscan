# Copy this file to .env and edit values for your local environment (do NOT commit .env with secrets)
LOCAL_DB_USER=postgres
LOCAL_DB_PASS=admin
LOCAL_JWT_SECRET=change-me-to-a-very-long-secret
# Optional: override host/port if you need to bind Postgres to a different port on host
LOCAL_DB_PORT=5434

